# Medicine no-RAG baseline, restored ESO, ja lm2 on aries45

## Hypothesis

Streaming no-RAG Qwen3-Omni on Japanese medicine samples provides the baseline
hypotheses and TERM_ACC denominator for strict-term review.

## Background / Motivation

Run the restored ESO medicine no-RAG baseline for:

```text
lang=ja
lm=2
samples=404 545006 596001 605000 606
compute=aries hold job 45290, physical GPUs 4,5
```

Input ESO test root:

```text
/mnt/taurus/home/jiaxingxu/rag-sst/eso-dataset/outputs_v2_abbrev_exact_match_abbrev_restored/test
```

Output root:

```text
/mnt/gemini/data1/jiaxuanluo/medicine_norag_baseline_abbrev_restored_batched_20260523_ja_lm2_aries45
```

## What changed vs baseline

This reuses the restored ESO batched no-RAG launcher, restricted to `ja` and
`lm=2`, with physical GPUs `4,5`.

## Expected metrics

Primary outputs are `instances.log`, `hypotheses.tsv`, `timing.tsv`, and the
StreamLAAL/TERM_ACC post-eval files generated by the baseline launcher.

## Verdict

Success.

Generation finished with `5/5` instances and wrote hypotheses plus timing:

```text
seconds=5536
minutes=92.267
```

Post-eval StreamLAAL / TERM_ACC:

```text
BLEU=23.8731
StreamLAAL=2143.40 ms
StreamLAAL_CA=2923.88 ms
TERM_ACC=0.4350
TERM_CORRECT=981
TERM_TOTAL=2255
miss_occurrences=1274
unique_missed_term_translations=684
```
