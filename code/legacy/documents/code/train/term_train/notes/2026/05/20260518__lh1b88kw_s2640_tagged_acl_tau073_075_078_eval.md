# lh1b88kw s2640 tagged ACL tau 0.73/0.75/0.78 readout

## Hypothesis

The dev-calibrated tau values can be reused without rerunning dev scoring. The missing readout is tagged ACL glossary behavior at tau 0.73, 0.75, and 0.78.

## Background / Motivation

Tagged ACL uses the existing tagged glossary rather than the paper-extracted ACL glossary. The tau choices come from prior dev calibration, while tagged ACL remains a held-out readout.

## What changed vs baseline

This is an eval-only tagged-ACL run from the `lh1b88kw` step-2640 checkpoint. It leaves dev, paper ACL, and medicine JSONL paths empty, uses the tagged ACL JSONL and tagged ACL glossary, and sweeps only tau 0.73, 0.75, and 0.78.

## Expected metrics

The run should report `eval_tagged_acl` recall, micro precision, and no-term noise for base, gs1000, and gs10000 banks. It should not run dev scoring or paper ACL/medicine scoring.

## Verdict

Completed in W&B run `nrxiasfm` through taurus hold job `45269` step `45269.5`.
The run evaluated only tagged ACL (`dev=0`, `acl=0`, `tagged_acl_dev=4943`,
`medicine_dev=0`) from the `lh1b88kw` step-2640 checkpoint.

Values are `Recall / P_micro / noise`.

| tau | base | gs1k | gs10k |
| --- | ---: | ---: | ---: |
| 0.73 | 97.92 / 19.08 / 1.01 | 97.95 / 14.96 / 1.88 | 97.90 / 10.15 / 7.19 |
| 0.75 | 97.52 / 21.31 / 0.73 | 97.54 / 17.38 / 1.26 | 97.67 / 10.68 / 5.49 |
| 0.78 | 96.60 / 25.08 / 0.46 | 96.63 / 22.03 / 0.70 | 97.19 / 12.88 / 3.04 |
