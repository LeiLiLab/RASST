# Figure 8: Term Duration Distribution

- Paper label: `fig:term_duration`
- LaTeX source: `latex/sections/appendix.tex`
- Paper output: `latex/figures/term_duration_dist.pdf`
- Paper-local plotting script: not recovered
- Data-prep script snapshot: `compute_term_duration_distribution.py`
- Local output snapshot: `term_duration_dist.pdf`

This appendix figure is currently a static copied PDF artifact. The copied
`compute_term_duration_distribution.py` script computes term-duration summary
data, but no reusable plotting script or local output data directory was found
in the repo during this reorganization. Treat this package as a provenance
placeholder until the plotting pipeline is backfilled.

Original provenance:

- Data-prep script:
  `../../../../../../tools/compute_term_duration_distribution.py`
- Repo-local script copy:
  `../../../../../../tools/compute_term_duration_distribution.py`
- Current paper PDF:
  `../../latex/figures/term_duration_dist.pdf`

If this figure needs editing, first backfill the exact data TSV/JSON and the
script that generated `term_duration_dist.pdf`.
